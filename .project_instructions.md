## Business Context Lock (May 2026)

### What changed
- **useActiveBusiness hook** (`hooks/use-active-business.ts`) — enhanced with `businessDomain` field computed via `extractDomain()` helper
- **Global header banner** (`app/components/header.tsx`) — sticky bar below header showing "Current Business: [name] · [domain]" on all authenticated pages; amber warning when no business selected
- **Dashboard card** (`app/dashboard/_components/dashboard-content.tsx`) — active business highlighted with blue border+ring; click triggers `setActiveBusiness()` with toast notification
- **sendToTombstone fix** (`app/dashboard/social/_components/social-dashboard.tsx`) — now passes `businessId: activeBusinessId` to `/api/social/generate`
- **Identity preflight** (`lib/tombstone.ts`) — `validateIdentityPreflight()` validates selected business matches identity lock before Tombstone commands; integrated into `createSocialMissions()`
- **Mismatch banner component** (`components/business-mismatch-banner.tsx`) — reusable red warning banner + `detectBusinessMismatch()` helper
- **Social Post Queue** — post cards show business name badge; amber mismatch warning when post.businessId ≠ activeBusinessId; API already filters by businessId
- **Posts API** (`app/api/social/posts/route.ts`) — includes `business` relation, exposes `businessName` and `businessWebsiteUrl` in response
- **Regression tests** (`__tests__/business-context.test.ts`) — 17 tests covering extractDomain, preflight validation, mismatch detection, API contracts, queue filtering, reference URL separation

## Post Import / Visibility Reconciliation (May 2026)

### Root cause fixed
`handleProgressComplete` used stale React state (`posts`) to verify import success — React hadn't re-rendered yet after `fetchPosts()` called `setPosts()`. Now uses `pollResult.imported` count from the API response + a fresh verification fetch.

### What changed
- **`handleProgressComplete`** (`social-dashboard.tsx`) — no longer uses stale `posts` state. Uses poll API `imported` count, then does a fresh fetch to `/api/social/posts` to verify the imported post is visible in the current business queue. If hidden by status filter or wrong business, shows specific diagnostic error and auto-clears filter.
- **`/api/social/missions/poll`** — enhanced import validation: validates required fields (caption, imageUrl, cta, source attribution), assigns `generation_incomplete` status for posts with image-only (no caption), returns `importedComplete`/`importedIncomplete` counts + `diagnostics` array with `missingFields`, `importError` per incomplete post.
- **`generation_incomplete` status** — new status type added to STATUS_CONFIG, filter buttons, and import logic. Posts with partial output (e.g., image but no caption) get this status instead of silently being `pending_approval`.
- **Diagnostic card** — `generation_failed` and `generation_incomplete` posts show an expanded diagnostic banner with: workflow ID, task ID, run ID, business name, source article, missing fields, and "Retry Import" / "View Workflow" buttons.
- **Frontend rendering guards** — normal posts show inline warnings for: missing caption, missing source attribution, missing workflowId link.
- **Retry import** — `retryImportForPost()` deletes the failed shell record, re-polls Tombstone, and refreshes posts.
- **Poll status message** — now shows "(X incomplete/failed)" suffix when incomplete imports exist.
- **Regression tests** (`__tests__/post-import-reconciliation.test.ts`) — 15 tests covering import validation, queue count consistency, malformed post visibility, business mismatch, post-import verification, and contradictory message prevention.

## Workflow ID Persistence Fix (May 2026)

### Root cause fixed
When generating social posts via `businessId` (rather than `analysisId`), the generate route resolved `websiteUrl` from the Business record but never looked up the associated Analysis. This left `resolvedAnalysisId` as null, so the `socialMissionId` (containing workflow IDs) was never stored on the Analysis record. The poll route uses `analysis.socialMissionId` to collect workflow IDs for querying the Tombstone content queue, so these workflows were invisible to polling — posts generated successfully but never imported.

### What changed
- **`/api/social/generate/route.ts`** — after finding the Business record via `businessId`, if `resolvedAnalysisId` is still null, now looks up the most recent Analysis for that business and user. This ensures `socialMissionId` gets written even for business-based generations (not just analysis-based ones).
- **`handleProgressComplete` retry logic** (`social-dashboard.tsx`) — added retry mechanism (5 attempts, 15-second intervals) when the first poll returns 0 imports after workflow completion. The Tombstone content queue can take time to be updated after workflow finishes.
- **Data fix** — manually patched North Shore Tavern's analysis record with 3 missing workflow IDs that were lost before the code fix.

## Draft-to-Post Enhancements (June 2026)

### Bug fix: Create-from-draft business assignment
- `create-from-draft/route.ts` was storing workflows on the wrong business's analysis (most recent analysis instead of selected business). Fixed to look up analysis by `businessId`.
- `create-from-draft/route.ts` now creates a `GenerationRun` record so poll route can map back to the workflow.
- Poll route (`missions/poll/route.ts`) now falls back to Tombstone `/tasks/{id}` API to get `workflow_id` when content queue entry lacks it.

### Article URL input (new feature)
- Draft form now has an **Article URL** input field — paste any web article URL
- Backend fetches article via `fetchAndParseArticle()`, builds carousel via `buildCarouselPackage()`, generates slide images via `generateCarouselImages()`, and creates SocialPost directly (no Tombstone polish needed)
- When both article URL and draft text are provided, draft text is prepended as "user angle/context" to the article body
- Response includes `{ immediate: true, postId }` — frontend refreshes post list immediately

### Carousel format for user-written drafts (new feature)
- Draft form has **Carousel Format** checkbox (auto-checked + disabled when article URL is present)
- When checked, `buildCarouselFromDraft()` sends draft text to LLM to detect list/event/tip content suitable for multi-slide format
- If LLM determines content IS carousel-worthy → generates slides + creates SocialPost directly
- If LLM determines content is NOT carousel-worthy → falls through to standard Tombstone polish pipeline (graceful degradation)
- Source type `user_draft_carousel` distinguishes these from article-based carousels

### Three paths in create-from-draft
1. **PATH 1**: `articleUrl` → article carousel (immediate)
2. **PATH 2**: `draftText` + `forceCarousel` → draft carousel if worthy, else falls to PATH 3 (immediate if carousel)
3. **PATH 3**: Standard draft → Tombstone polish mission (existing behavior)

## Identity Lock for Lane Missions (June 2026)

### Root cause fixed
`createLaneMission()` in `lib/tombstone.ts` was the ONLY mission creation function that lacked the `buildIdentityLockBlock()`. This meant the 3-lane analysis launch (website/news/holiday) sent commands to Tombstone WITHOUT an advertiser identity lock. When Tombstone agents processed these commands, they could pick up identity from adjacent/recent missions for other businesses (e.g., Blazin Hog CTA appearing on West Highlands Auto Repair ads).

### What changed
- **`createLaneMission()`** — now accepts optional `businessName` parameter and prepends `buildIdentityLockBlock()` to ALL lane commands (website, news, holiday), matching the pattern used by `createSocialMissions()`, `createSingleStoryPost()`, and `createWeeklyTipPost()`.
- **`confirm-and-launch/route.ts`** — passes `businessName` to all 3 `createLaneMission()` calls.
- **`generate-more/route.ts`** — passes `businessName` to `createLaneMission()` call.
- **Data fix** — corrected contaminated CTA on West Highlands Auto Repair analysis (was "Check your address at blazinghog.co", now "Schedule Your Service").

### Shared Asset Library (June 2026)
- **Feature**: Reusable shared asset layer alongside per-business Creative Assets
- **Schema**: 6 new models — `SharedAsset`, `SharedAssetPack`, `SharedAssetPackItem`, `BusinessSharedAssetApproval`, `BusinessSharedAssetPackGrant`, `SharedAssetAuditLog`. Relations added to Business and User.
- **Scopes**: global, industry, brand_oem, franchise, licensed_stock, template
- **Categories**: industry_generic, licensed_stock, brand_oem, franchise, templates, video_clips, audio_clips, icons_graphics, educational_explainers, compliance_templates
- **License model**: licenseType, licenseStatus, licenseExpiry, rightsHolder, attributionRequired/Text, 8 channel permission booleans, requiresApproval (brand_oem defaults to true), noDerivatives, noCommercial, geographicRestriction, industryRestriction
- **Admin-only**: SharedAsset creation/upload restricted to role=admin
- **Deduplication**: sha256Hash field on SharedAsset
- **Audit log**: SharedAssetAuditLog for all operations
- **API Routes**: `/api/shared-assets/*`, `/api/shared-asset-packs/*`, `/api/businesses/[id]/shared-assets/approvals`, `/api/businesses/[id]/shared-asset-packs/grants`, `/api/shared-assets/allowed`
- **Helper**: `getAllowedAssetsForBusiness(businessId, intendedUse, agentType)` in `lib/shared-assets.ts` — shared asset permission layer
- **Agent Asset Layer**: `lib/agent-assets.ts` — `getAgentAssets()` wrapper with guardrails, ranking, logging. Wired for website, seo, social, video, community_engagement agents.
- **Agent Asset API**: `GET /api/agent-assets`, `GET /api/agent-assets/usage` — permission-safe agent asset retrieval + usage logs
- **AgentAssetUsageLog** model: tracks businessId, agentType, intendedUse, retrieved/used/skipped assets per workflow
- **UI**: `AssetAgentUsage` component on asset edit modal shows "Used by agents" summary
- **Convenience wrappers**: `getWebsiteAssets()`, `getSeoAssets()`, `getSocialAssets()`, `getVideoAssets()`, `getCommunityEngagementAssets()`

### Tombstone Asset Bridge (June 2026)
- **Feature**: Centralized bridge (`lib/tombstone-asset-bridge.ts`) between Launch OS asset permission layer and Tombstone generation workflows
- **Core function**: `buildAssetContextForMission(businessId, agentType, opts?)` → returns `AssetBridgeResult` with:
  - `context`: `TombstoneAssetContext` (compact assets, skipped list, logId, counts)
  - `promptBlock`: Text injection block with guardrails for text-command workflows
  - `hasAssets`: boolean flag
- **Types**: `TombstoneAssetPayload` (compact shape), `TombstoneSkippedAsset`, `TombstoneAssetContext`, `AssetBridgeResult`
- **Convenience wrappers**: `buildWebsiteAssetContext()`, `buildSeoAssetContext()`, `buildSocialAssetContext()`, `buildVideoAssetContext()`, `buildCommunityEngagementAssetContext()`
- **Wired into all Tombstone dispatch functions** in `lib/tombstone.ts`:
  - `buildLaneCommands()` — 3 lane commands (website_post, evergreen_post, news_post)
  - `createSocialMissions()` + `buildStoryCommand()` — 4 story variants
  - `createLaneMission()` — 3 lane types (website, news, holiday)
  - `createDraftPolishMission()` — accepts businessId option
  - `createScoutStoryMission()` — uses meta.businessId
  - `createWeeklyTipMission()` — uses opts.businessId
- **Wired into website routes**:
  - `generate-concept-site/route.ts` — passes `asset_context` in structured payload
  - `site-revision/route.ts` — passes `asset_context` in structured payload
  - `create-from-draft/route.ts` — passes `businessId` to `createDraftPolishMission()`
- **Pattern**: Text-command workflows get `promptBlock` appended; structured payload workflows get `asset_context` field
- **Safety**: All fetches wrapped in try/catch — generation continues without assets if bridge fails
- **Guardrails in prompt block**: Do not invent/reference assets outside list, do not use blocked assets, respect attribution, prefer business-owned over shared
- **Asset grouping**: Logos, Photos, Videos, Audio, Documents, Compliance/Restrictions, Shared Assets, Other
- **Tests**: `__tests__/tombstone-asset-bridge.test.ts` — 20 tests covering core function, compaction, prompt block, convenience wrappers, error handling
- **No schema changes**: Bridge uses existing `getAgentAssets()` API, no direct DB queries from Tombstone
- **Commit**: `5e0f6e2`
- **Guardrails**: publicUseAllowed, aiUseAllowed, customerPermission, noDerivatives, noCommercial, expired/revoked license, cross-business isolation all enforced
- **Ranking**: business assets > approved shared brand/OEM > industry > global/template; topic/tag matching, quality scoring
- **UI**: 4-tab layout on `/dashboard/assets`: Business Assets | Shared Assets | Approved Asset Packs | Usage Rules
- **Constants**: `lib/shared-assets.ts` — scopes, categories, license types, statuses, channel labels, field maps

### Creative Asset Library (June 2026)
- **Feature**: Registered-owner-only creative asset library at `/dashboard/assets`
- **Schema**: `BusinessAsset` model added to Prisma with full metadata, approval workflow, text-content support
- **Access Control**: `lib/asset-access.ts` — `verifyAssetAccess()` enforces that `Business.userId` is not null AND the current user is the owner or admin. Anonymous tokens, provisional tombstoneBusinessId alone, and unregistered businesses are all blocked (403).
- **Validation**: `lib/asset-validation.ts` — Per-category file type/size limits (logo ≤1MB, photos ≤5MB, creative examples ≤10MB, etc.), SVG sanitization (strips scripts, event handlers, foreign objects, external URLs), forbidden file types.
- **Storage**: Uses existing S3 cloud storage via `lib/aws-config.ts` + `lib/s3.ts`. **Cloudflare gap**: The spec requests Cloudflare R2 storage. Current implementation uses the available S3 infrastructure with a clean abstraction layer. To migrate to Cloudflare: update `lib/aws-config.ts` and `lib/s3.ts` to use Cloudflare R2 SDK, or proxy uploads through Tombstone's R2 endpoints.
- **API Routes**:
  - `POST /api/assets/upload` — Creates asset record + returns presigned S3 URL for direct upload (or creates text assets inline)
  - `POST /api/assets/upload/confirm` — Confirms upload, SVG sanitization, generates public URL
  - `GET /api/assets?businessId=x&category=y&status=z` — Lists assets for a business (owner/admin only)
  - `PATCH /api/assets/[id]` — Updates metadata, approval status
  - `DELETE /api/assets/[id]` — Deletes asset and storage file
  - `GET /api/assets/eligibility?businessId=x` — Quick eligibility check for UI
  - `GET /api/assets/approved?tombstoneBusinessId=x&adObjective=trust` — Tombstone-facing endpoint for retrieving approved assets during creative workflows. Supports filtering by category, type, and ad objective (trust, product, local, brand). Always includes brand assets. Authenticated via ADMIN_API_KEY header or session.
- **UI**: `/dashboard/assets` with category-grouped asset cards, drag-and-drop upload modal, edit modal, search/filter, approval workflow buttons
- **Nav**: "Assets" tab added to header nav for authenticated users
- **Categories**: brand, products_services, people_trust, location_service_area, proof_social_proof, compliance, creative_examples
- **Text Assets**: approved_claim, forbidden_claim, disclaimer, font_notes, color_palette — stored as text content without file upload
- **Approval Statuses**: uploaded → pending_review → approved / rejected / do_not_use / expired / archived. Only `approved` assets are served to Tombstone.

### Scout Geo Fallback Cascade (June 2026)
- **Problem**: Local news came up blank for businesses outside the seeded TX geo area (e.g., Buffalo NY). Only ZIP-radius search via FeedGeo existed; no geographic widening when results were thin.
- **Root cause**: `FeedGeo` table had 17,416 rows all concentrated on TX ZIPs. Zero entries for NY, and no `pilotState='NY'` feeds.
- **Fix**: `generateContentBriefWithFallback()` in `lib/rss/trade-area-feed.ts` implements a 5-level cascade:
  1. **ZIP radius** (initial lookback days)
  2. **City** (2× lookback via `getTradeAreaItems({ cities })`)
  3. **County** (full LOOKBACK_DAYS via `getTradeAreaItems({ counties })`)
  4. **State** (full LOOKBACK_DAYS via `getTradeAreaItems({ states })`)
  5. **National-only** (queries `geoScope='national'` feeds directly)
- **Auto-discovery**: When results < `SCOUT_MIN_LOCAL_ITEMS` after all levels, `triggerAsyncDiscovery()` probes candidate news sites (cityname+news/patch/press/times/etc.) and saves discovered feeds with `status: 'pending'` for manual activation.
- **Env vars**: `SCOUT_MIN_LOCAL_ITEMS=5`, `SCOUT_MAX_LOCAL_ITEMS=20`, `SCOUT_LOCAL_NEWS_LOOKBACK_DAYS=14`, `SCOUT_FEED_DISCOVERY_ENABLED=true`
- **Diagnostics**: `BriefDiagnostics` interface tracks fallbackLevel, levelsAttempted, discoveryTriggered, queryTimeMs. Exposed in Clark Kent's `meta.rssDiagnostics`.
- **Re-ranking**: Composite score includes geo specificity bonus (zip:10, city:7, county:4, state:2, national:1), feed diversity cap (max 3 per feed), title dedup.
- **Clark Kent route**: Now calls `generateContentBriefWithFallback()` with city/state context. Scout summary shows `[geo fallback: county]` notes and per-headline `[locality_level]` tags.
- **Backwards compatible**: `generateContentBrief()` still works (delegates to fallback version). `ContentBrief` interface has optional `diagnostics` and `localityLevel` fields.
- **Key helpers used**: `getZipDetails()` resolves ZIP → city/county/state. `getTradeAreaItems()` already supported `cities`, `counties`, `states` params.

#### Geo Linker & Feed Discovery Pipeline (June 2026)
- **`lib/rss/geo-linker.ts`** — comprehensive feed discovery + validation + FeedGeo linking:
  - `validateFeed()`: Fetches feed, parses XML, verifies item count/recency. Invalid feeds rejected before saving.
  - `createFeedGeoLinks()`: Creates `FeedGeo` rows linking a feed to all ZIPs in a city/county/state scope. Uses existing `getZipsByCity`/`getZipsByCounty`/`getZipsByState`.
  - `inferGeoFromFeed()`: Deterministic geo inference from known-domain map (`CITY_DOMAIN_MAP`), Patch.com URL pattern, state names in title, `pilotState` field.
  - `discoverValidateAndLink()`: Full pipeline: build candidate site list → discover feeds via HTML+path probing → validate each → save `RssFeed` → create `FeedGeo` links. Used by both async scout discovery and manual admin trigger.
  - `backfillFeedGeo()`: For existing feeds without `FeedGeo` rows, infers geo and creates links. Reports uncertain feeds instead of guessing.
  - `getGeoCoverageReport()`: Analytics showing coverage gaps, uncovered states, location-specific diagnostics.
  - **Known-domain registries**: `KNOWN_CITY_DOMAINS` (Buffalo, Rochester, Syracuse, Dallas, etc.), `KNOWN_STATE_DOMAINS` (NY, TX, FL, CA, etc.), `CITY_DOMAIN_MAP` for domain→city inference.
- **Discovery creates FeedGeo links**: Every discovered local feed gets linked to the best available geo scope (city ZIPs > county ZIPs > single ZIP fallback). This is the CRITICAL fix that was missing — without FeedGeo rows, the trade-area query can't find feeds.
- **Validated feeds go `status: 'active'`**: Unlike the old discovery which saved as `pending`, validated feeds are immediately active and available for queries.
- **De-duplication**: Items now de-duped by both ID and normalized title (lowercase, stripped punctuation, first 80 chars). Prevents duplicate headlines from overlapping feeds.
- **Admin endpoint**: `GET /api/rss/admin/geo-coverage?zip=14203&city=BUFFALO&state=NY` — coverage report. `POST` with `action=discover` for manual discovery trigger or `action=backfill` for geo inference of existing feeds.

#### Split-Brain Fix & Local Feed Cron Worker (June 2026)
- **Split-brain fix**: `geo-linker.ts` and `geo-lookup.ts` were importing `prisma` from `@/lib/db` (primary app DB) while Scout reads from `rssPrisma` (tombstone DB). This meant `ingestFeeds()` and `discoverValidateAndLink()` wrote items to the wrong database. Fixed by changing both files to `import { rssPrisma as prisma } from '@/lib/rss-db'`. All RSS/geo tables exist in both DBs with identical schema.
- **content-policy.ts stays on primary DB**: The `ContentPolicy` table only exists in the primary DB. `content-policy.ts` must keep `import { prisma } from '@/lib/db'`.
- **Cron endpoint**: `POST /api/rss/cron/poll-local-feeds` — recurring local feed polling worker:
  - Polls active local/state/city feeds only (not national — those use `poll_national_feeds.py`)
  - Auth: `x-cron-secret` header (CRON_SECRET) or `x-api-key`/`Authorization` (ADMIN_API_KEY)
  - Optional body: `{ state?, city?, county?, limit?, concurrency? }` to scope the poll
  - Dedup by feedId+guid unique constraint + cross-feed content-hash near-duplicate detection
  - Updates `lastFetchedAt` + resets `consecutiveErrors` on success, increments on failure
  - Auto-disables feeds after `SCOUT_FEED_FAILURE_DISABLE_THRESHOLD` (default 5) consecutive errors
  - Returns health snapshot: activeLocalFeeds, staleFeedCount24h, newestLocalPubDate, feedsByGeoScope
  - `GET` returns health probe only (no ingest)
  - Env vars: `CRON_SECRET`, `RSS_CRON_BATCH_SIZE` (200), `RSS_CRON_CONCURRENCY` (5)
- **Render cron setup**: Add a Render Cron Job that calls: `curl -s -X POST https://connect.launchmarketing.com/api/rss/cron/poll-local-feeds -H "x-cron-secret: $CRON_SECRET" -H "Content-Type: application/json"` every 4-6 hours. Set `CRON_SECRET` and `TOMBSTONE_DATABASE_URL` in Render env vars.
- **Coverage stats** (as of June 2026): 574 feeds total (261 active), 31,744 FeedGeo links, 9 states with local coverage (TX:126, FL:86, CO:83, MT:82, NC:79, NY:9, OH:2, MO:1, GA:1). Colorado Springs: 3 active feeds (Gazette Atom, ElPasoCo.com ×2).

### Progressive Mission Visibility → Website Production Room (June 2026)
- **Feature**: Full customer-facing Website Production Room — replaces simple progress tracker with production workspace
- **Backend** (`tombstone` repo):
  - `migrations/035_mission_events.sql`: Append-only `mission_events` table
  - `core/mission_events.py`: Event emission helpers, build_progress_response() for structured progress payload
  - `server/mission_control_api.py`: `GET /workflows/{workflow_id}/progress` endpoint
  - Agent→artifact mapping: Jim Bridger→business_research, Zig Ziglar→strategy_brief, David Ogilvy→copy_deck, Don Draper→image_direction, Andy Warhol→rendered_images, George Boole→website_preview, Peter Drucker→qa_report
- **Frontend** (`ad-launch` repo, commit `e176635`):
  - `app/api/workflow-progress/route.ts`: Extended to include `artifact_details` (parsed task outputs) when `includeArtifacts=true`. Uses `extractArtifactSummary()` to produce customer-friendly summaries per artifact type.
  - `app/components/website-production-room.tsx`: **New** WebsiteProductionRoom component:
    - **Production Header**: Dark gradient header showing business name, current phase, status, available artifacts, next decision, progress %
    - **Activity Ribbon**: Customer-safe messages (no agent names) — "Researching your business…", "Building your website strategy…", etc.
    - **Production Timeline**: Vertical stages with functional labels, status badges, elapsed time, inline View/Hide artifact expansion
    - **Progressive Artifact Cards**: Inline-expanded parsed task outputs for each completed stage (business research, strategy brief, sitemap, copy deck, image direction, QA report)
    - **Review Gates**: Approve / Request Changes controls for Sitemap & Services, Copywriting, Creative Direction — routes feedback into existing `/api/site-feedback` system
    - **Operator Diagnostics**: Admin-only collapsible panel showing internal agent names, task IDs, retries, errors
  - **Agent name mapping**: All internal agent names mapped to customer-facing functional labels:
    - Jim Bridger → Business Research, Zig Ziglar → Website Strategy, David Ogilvy → Copywriting, Don Draper → Creative Direction, Andy Warhol → Image Production, George Boole → Website Assembly, Peter Drucker → Quality Review
  - `app/components/mission-progress.tsx`: Retained for backward compatibility but no longer imported
  - `website-concept.tsx`: Uses WebsiteProductionRoom as primary workflow display, WorkflowProgress as fallback before workflowId exists
  - Polls every 5s with auto-expand of newly available artifacts

### Competitor-Informed Website Concept Generation (June 2026)
- **Feature**: Users can optionally provide up to 3 competitor website URLs during website concept generation. The system deep-analyzes competitors, generates 3 distinct concepts informed by competitive intelligence, evaluates them in a War Room, picks a winner, and produces a final optimized site plan.
- **Backend** (`tombstone` repo, commit `8e6c4eb`):
  - `core/competitor_analyzer.py` (~806 lines): `deep_analyze_competitor()` (5-phase: crawl, SEO, offers, SWOT, traffic pages), `build_competitive_synthesis()`, `generate_three_concepts()` (offer-led/trust-led/SEO-led), `evaluate_concepts_war_room()`, `build_final_site_plan()`, `run_competitor_analysis_pipeline()`, DB persistence functions
  - `migrations/028_competitor_analyses.sql`: `competitor_analyses` and `competitive_syntheses` tables
  - `agents/jim_bridger.py`: When competitor URLs provided, scrapes each (max 3), runs full pipeline, adds `competitor_intelligence` to BrandAssetRecon output. Maintains backward-compatible `competitive_seo_recon`
  - `server/mission_control_api.py`: `competitor_intel_block` context string threaded into workflow steps 2 (Zig), 3 (Ogilvy), 7 (Boole). New `GET /competitor-analysis/{workflow_id}` endpoint
- **Frontend** (`ad-launch` repo, commit `5c7c862`):
  - `website-concept.tsx`: Competitor URL limit 3, URL validation, `competitorIntel` state, Competitor Intelligence Panel UI (concept cards, War Room scores, winner badge, decision reasons, competitive gaps)
  - `generate-concept-site/route.ts`: Cap at 3 competitor URLs
  - `competitor-analysis/route.ts`: API proxy to Tombstone backend
  - `tombstone.ts`: `getConceptWebsiteStatus()` extracts `competitorIntelligence` from Research (Jim Bridger) task output
- **Tests**: `__tests__/competitor-analysis.test.ts` (~350 lines) covering URL validation, sanitization, data structures, 3-concept structure, War Room evaluation, content safety, business isolation, fallback behavior
- **Key design decisions**: Competitor analysis runs WITHIN Jim Bridger's recon step (Step 1), not as separate workflow steps. Peter Drucker's War Room (Step 8) is for generated HTML quality, not concept selection. Falls back gracefully to existing behavior when no competitors provided.

### Upgraded Website Generation Pipeline (June 2026)
- **Tombstone workflow steps upgraded**:
  - Step 1 (Jim Bridger): Service/product discovery + owner feedback passthrough
  - Step 2 (Zig Ziglar): Website strategy brief with structured fields (primary_business_type, target_customer, core_pain_points, service_taxonomy, seo_sitemap, homepage_section_plan)
  - Step 3 (David Ogilvy): Section contracts FIRST, then benefit-led copy deck. Forbidden copy patterns enforced.
  - Step 4 (Don Draper): Image strategy briefs per section (image_purpose, audience_takeaway, must_show, must_avoid, visual_style, composition, text_guidance, mobile_crop_risk)
  - Step 7 (George Boole): Section contract enforcement, service page architecture, nav/header rules (desktop/mobile), responsive QA, layout QA
  - Step 8 (Peter Drucker): 20-gate comprehensive QA (10 hard-fail, 5 strategy alignment, 5 polish)
- **Owner feedback flow**: owner_feedback threaded through all steps via feedback_block in execution_notes
- **Schema**: SiteFeedback model (analysisId, sectionId, target, feedback, requestedAction, status, appliedAt, appliedInWorkflowId, userId)
- **API Routes**: /api/site-feedback (CRUD), /api/site-revision (trigger revision with feedback), /api/site-diagnostics (strategy brief, section contracts, image briefs, QA gates)
- **Frontend**: SectionFeedbackButton per section, FeedbackPanel with Apply & Regenerate, DiagnosticsPanel (strategy brief, section contracts, image strategy, QA gates)
- **ConceptWebsitePayload**: Added owner_feedback array for revision workflows

### Infrastructure Source of Truth (June 2026)
- **Production frontend**: `connect.launchmarketing.com` → Render (`ad-launch-frontend.onrender.com`), verified on Render. GitHub → Render is the production deploy path.
- **Legacy/dev preview**: `ad-launch-1nfyr8.abacusai.app` → Abacus AI. NOT production. Do NOT refer to Abacus-hosted app as production.
- **Production backend**: `tombstone-api` → Render Standard (`tombstone-api-xjc4.onrender.com`)
- **Workers**: `tombstone-worker`, `tombstone-publish-worker` → Render
- **CRM**: GHL / Launch CRM — infrastructure, not the customer portal
- **Product name**: Launch OS (not Launch Connect, not Ad Launch)
- **Deploy path**: Code → GitHub → Render auto-deploy. Abacus checkpoint/deploy is for legacy/dev only.
- **Do not recommend removing the Render frontend**
- **Do not assume Abacus AI hosts production**
- **Do not say "deploy from here to update" as if Abacus is production**
- **Full doc**: `/home/ubuntu/tombstone/docs/INFRASTRUCTURE_SOURCE_OF_TRUTH.md`

### GHL Credential Boundary (June 2026)
- **Architecture**: Strict two-level credential isolation enforced:
  - Agency PIT: provisioning-only (create sub-account, generate/retrieve sub-account PIT, read agency metadata)
  - Sub-account PIT: all location operations (Social Planner, contacts, forms, workflows, calendars, user lookup)
- **Agency fallback removed**: `_build_location_client()` in `ghl_publisher.py` no longer falls back to agency token. Returns `manual_action_required` if no sub-account credential exists.
- **Business mapping fields** (migration 034): `ghl_credential_type`, `ghl_credential_status`, `social_provider`, `facebook_connection_status`, `default_ghl_user_id`, `default_ghl_user_name`, `default_social_account_id`, `last_verified_at`
- **Default user selection**: Deterministic chain — persisted default → admin preference → first active user → persist chosen. oauthId fallback only for Social Planner when GET /users/ returns 0.
- **User lookup**: Always use `GET /users/?locationId=...` (not `/users/search` which is agency-scoped and returns 0 with sub-account PITs)
- **E2E verified**: Blazing Hog (lTFB8cQnb8hAyTMouru7) — staff userId works for Social Planner posting, 4 users confirmed, Bruce Hackfeld auto-selected as default.
- **New endpoints**: `/admin/integrations/ghl/credential-boundary/{business_id}`, `/admin/integrations/ghl/set-default-user`
- **Tombstone commit**: `b101187`

### Secondary Text Lines in Image Renders (June 2026)
- **Problem**: When users write drafts with specific data (dates, schedules, stats), those facts only appeared in the caption, not the rendered image. Users expected key data like game dates to be visible in the creative itself.
- **Fix**: Added `secondary_text_lines` support across the full pipeline:
  - `createDraftPolishMission()` in `lib/tombstone.ts` now instructs the pipeline to extract key data points as secondary text
  - Don Draper's creative direction JSON schema includes `secondary_text_lines` field
  - Andy Warhol's `_build_full_comp_prompt()` renders secondary text lines between headline and CTA
  - Both multi-campaign and single-campaign render paths pass `secondary_text_lines` through
- **Also**: Added instruction for people/subjects to face TOWARD the action (e.g., fans facing TV, not away from it)

### Post Scheduler Module (June 2026)
- **Feature**: Full scheduling pipeline — after post generation, users can schedule posts via a multi-step onboarding wizard and manage them from a dedicated scheduling dashboard.
- **Schema additions** (compatible, no data loss):
  - `ScheduledPost` model: businessId, userId, socialPostId, caption, imageUrl, hashtags, cta, platforms[], scheduledFor, timezone, status (9 states), approvalRequired, approvedById/At, revisionRequestText/At/Count, publishedAt, failureReason, publishAttempts, lane, sourceType
  - `SocialConnection` model: businessId, platform, externalAccountId, displayName, profileUrl, accessToken (encrypted), refreshToken, tokenExpiresAt, permissionsStatus, lastCheckedAt/PublishedAt, errorMessage, isActive. Unique on [businessId, platform].
  - `PublishSettings` extended: postingGoal, approvalMode (review_first/auto_after_approval/full_autopilot), cadence (light/standard/growth/custom), preferredDays[], preferredTimes[], defaultChannels[], autopilotEnabled, onboardingComplete
- **Scheduling utility** (`lib/scheduling-utils.ts`): POSTING_GOALS, APPROVAL_MODES, CADENCE_CONFIG (light=2/wk, standard=3/wk, growth=5/wk), STATUS_LABELS (9 customer-facing labels), PLATFORM_CONFIG (5 platforms, TikTok coming soon), business type→goal mapping, generateRecommendedSchedule(), buildScheduledPostsData(), getNextAvailableSlots(), formatScheduleDate()
- **API endpoints**:
  - `PUT /api/businesses/[id]/posting-preferences` — upsert posting settings
  - `GET /api/businesses/[id]/posting-preferences` — read settings
  - `GET/POST /api/businesses/[id]/social-connections` — list/create social connections
  - `POST /api/businesses/[id]/schedule-posts` — create scheduled posts from existing SocialPosts
  - `GET /api/businesses/[id]/scheduled-posts` — list with status filter, pagination
  - `POST /api/scheduled-posts/[id]/approve` — approve single or all (approveAll=true)
  - `POST /api/scheduled-posts/[id]/revision` — request revision with feedback text
- **Completion screen CTA** (`results-content.tsx`): "Schedule These Posts" button opens wizard; after scheduling shows success state with link to dashboard
- **Scheduling Wizard** (`components/scheduling-wizard.tsx`): 5-step modal — Channels → Goal → Approval Mode → Cadence → Review Schedule. Creates posting preferences, social connections, and scheduled posts on submit.
- **Schedule Dashboard** (`/dashboard/social/schedule`): 4 tabs — Queue (filterable post list with approve/revision actions), Calendar (date-grouped timeline), Approvals (filtered needs_approval with Approve All), Connections (platform cards with status)
- **Header nav**: "Schedule" tab added with CalendarClock icon
- **Tests**: `__tests__/scheduling.test.ts` — 30+ tests covering all constants, schedule generation, cadence mapping, status labels, business type→goal defaults, helpers
- **Design language**: "Your AI marketing team" not "manage your queue". Always show recommended schedule first, never blank scheduler.
- **NOT implemented** (per spec): email-based approval, full autopilot mode (disabled in UI), drag-and-drop calendar, 90-day auto-fill, actual OAuth flows for social platforms (connections are placeholder)

### Top-Level Navigation Restructure (June 2026)
- **Header rewritten** (`app/components/header.tsx`): New nav structure:
  ```
  Launch OS | Business ▾ | Search | Create ▾ | Social | Website | SEO | Insights | Account ▾
  ```
- **Create dropdown**: Scout Story, Weekly Tip, My Own Post, Article Carousel (link to `/dashboard/social?action=X`), plus disabled items: Website Content, Ad Creative, Email Campaign, Upload
- **Account dropdown**: Dashboard, Brand Assets, Billing/Credits, Content Feeds, Admin (conditional), Sign Out
- **Business selector**: Inline dropdown in nav bar (replaced old banner below header)
- **Alert badges**: Real counts for Social (from SocialPost DB), placeholder zeros for Website/SEO/Insights. Severity colors: red (≥5), amber (≥1), blue (0). Endpoint: `GET /api/nav/alerts`
- **Mobile**: Full hamburger menu with all nav sections
- **New routes**:
  - `/dashboard/website` — placeholder "Coming Soon" cards
  - `/dashboard/seo` — placeholder "Coming Soon" cards
  - `/dashboard/insights` — redirects to `/dashboard/marketing`
- **Social Posts page**: 4 large creation buttons replaced with compact `CreatePostDropdown` component; "Sync from Tombstone" kept persistent
- **No backend/workflow changes** — frontend nav only

### Visual Residue / Cross-Business Image Contamination (June 2026)
- **Problem**: Generated website concepts and social posts sometimes contain imagery from unrelated businesses (e.g., wifi/router/ISP imagery appearing on a cupcake shop's website). This is cross-business context contamination in the image generation pipeline.
- **Root cause**: Tombstone backend — Don Draper's image direction and Andy Warhol's render prompts may pick up cached context, memory, or asset references from prior workflows for different businesses. The identity lock block prevents TEXT contamination but doesn't scope IMAGE generation prompts.
- **Where to fix (Tombstone backend)**:
  1. Don Draper (`agents/don_draper.py`): Image direction schema must include `business_id` scoping. The `must_show` and `must_avoid` fields should be validated against the current business's asset library only.
  2. Andy Warhol (`agents/andy_warhol.py`): Render prompt should include explicit business-scoped image context (business name, industry, website) to prevent cross-contamination.
  3. Memory service: Ensure `business_memory` lookups are scoped by `business_id` and never return assets/context from other businesses.
  4. QA gate (Peter Drucker): Add a visual coherence check — flag renders whose content doesn't match the advertiser's industry/domain.
- **Frontend side**: No fix needed. Asset retrieval (`/api/assets/approved`) is already properly scoped by `businessId`/`tombstoneBusinessId`. The contamination happens during prompt construction on the Tombstone side.
- **Mitigation added**: Enhanced logging in `getWorkflowResults()` and `mission-status` to track lane→workflow→ad mappings for post-incident diagnostics.

### WordPress Demo Deployment System (June 2026)
- **Feature**: Publishes generated website concepts as live demo pages on `geoprofit.ai` via WP REST API
- **Backend** (`tombstone` repo, commit `69444a8`):
  - `core/wp_deploy_config.py` (~200 lines): `DemoTarget` dataclass, `normalize_slug()`, `make_collision_safe_slug()`, `is_reserved_slug()`, `validate_config()`, `load_demo_target()`. Env vars: `WP_DEMO_BASE_URL`, `WP_DEMO_USERNAME`, `WP_DEMO_APP_PASSWORD`, fallback `WP_DEPLOY_DOMAIN`
  - `core/wp_demo_publisher.py` (~400 lines): `DemoPublisher` class — `validate_credentials()`, `deploy_concept()` (idempotent create/update), `upload_media()`, `remove_demo()`, `persist_deployment()`. Uses business_id tracking tag in excerpt for collision detection vs idempotent updates
  - `migrations/029_demo_deployments.sql`: `demo_deployments` table (business_id, slug, domain, live_url, status, result_json) with UNIQUE(business_id, domain) for upsert
  - `config/settings.py`: Added `wp_demo_*` fields
  - `server/mission_control_api.py`: `GET /demo-deploy/validate` (admin-only credential check), `GET /demo-deploy/deployments` (list by domain/status)
  - `tests/test_wp_demo_deploy.py`: 37 tests covering config, slugs, collisions, security, full deployment flow
  - `docs/WP_DEMO_DEPLOY_PHASE1.md`: Full documentation
- **URL strategy**: `https://geoprofit.ai/{business-slug}/`, max 60 chars, reserved slugs auto-prefixed with `demo-`
- **Security**: App passwords never in logs, repr, or API responses. All `to_safe_dict()` methods strip credentials
- **Separate from production WP**: Uses `WP_DEMO_*` prefix env vars, distinct from existing `WP_BASE_URL`/`WP_USERNAME`/`WP_APP_PASSWORD` used by SEO publisher
- **Pending**: WordPress installation on geoprofit.ai, Application Password creation, `WP_DEMO_USERNAME` and `WP_DEMO_APP_PASSWORD` env vars in Render
- **Server**: HostGator `162.214.76.252`, cPanel account `geoprofit`, MySQL DB `geoprofit_wp` created, PHP 7.4 available

### Industry Service Taxonomy & SEO Service-Page Generation (June 2026)
- **Feature**: Reusable industry taxonomy -> prefilled per-business service checklist -> owner confirm/edit/reject -> confirmed services power SEO/ads/social + can generate draft service web pages and explainer video briefs.
- **Schema** (additive, pushed via `prisma db push`): `Industry` (name, slug, matchKeywords[], gbpCategories[], self-relation hierarchy), `IndustryService` (per-industry catalog; conditional flag; SEO + explainer-video template fields; @@unique[industryId,slug]), `BusinessServiceOffering` (per-business; status suggested|confirmed|rejected|hidden|needs_review; source; confidence; priority primary|secondary|optional|do_not_promote; seoEnabled; pageStatus; videoStatus; bridgerEvidenceJson; generatedPageJson; videoBriefJson; @@unique[businessId,slug]). `Business` got matchedIndustryId/Confidence/Source/Evidence + ownerConfirmedIndustry. `User` got confirmedServiceOfferings relation ("ServiceConfirmer").
- **Seed**: `scripts/seed-industries.ts` (idempotent upsert, no deletes) — run `yarn tsx --require dotenv/config scripts/seed-industries.ts`. Auto Repair (33), Dental (14), HVAC (13) + 12 placeholder industries = 15 industries, 60 services. SEPARATE from canonical seed.ts.
- **Core lib** `lib/industry-services.ts`: `matchBusinessToIndustry(businessId, bridgerResearch)` (owner-confirmed short-circuits; GBP cat=6, keyword=1.5 capped 8, name=2; >=8 high/>=4 medium/else low), `prepopulateServicesFromIndustry()` (conditionals only with evidence; evidence->confirmed/needs_review; never touches owner-confirmed/rejected/hidden), `runIndustryMatchAndPrepopulate()`, `getConfirmedServicesForSeo(businessId)` (the SEO/ads/social gate — filters do_not_promote + !seoEnabled), `offeringDisplayName()`.
- **Content gen** `lib/service-content-generator.ts`: `generateServicePage()` -> URL structure `/services/[slug]` + multi-location `/locations/[loc]/services/[slug]`, pageStatus='needs_review' (NEVER auto-published). `generateExplainerVideoBrief()` -> 30-60s vertical 1080x1920 brief+script. Uses claude-sonnet-4-6.
- **Jim Bridger hook**: `app/api/scout/completion-check/route.ts` calls `runIndustryMatchAndPrepopulate()` (non-blocking) after `saveBusinessProfile()`.
- **API**: `/api/businesses/[id]/services` (GET grouped/POST custom), `/services/[offeringId]` (GET/PATCH/DELETE — confirming sets ownerConfirmed+source=owner_confirmed; owner wins over research), `/generate-page`, `/generate-video-brief`, `/match-industry`, `/seo-index` (x-agent-key=AGENT_INTERNAL_API_KEY for server-to-server). Admin: `/api/admin/industries/*`.
- **UI**: `/dashboard/website/services` (ServicesManager). Admin "Industries" tab (`industries-tab.tsx`). Help Me Build It Services step shows prefilled `ServicesChecklistPanel` (in business-profile-interview.tsx) replacing blank coreServices question when services exist. Header nav: "Services Offered" in Website & SEO dropdown + mobile.
- **Commit**: b835503 on main.

### Tracking Pixels & Funnel Event Tracking (June 2026)
- **Feature**: Centralized, business-scoped tracking configuration system under `Insights → Tracking Pixels` (`/dashboard/insights/tracking-pixels`). Stores pixel/tag installs, conversion events, and retargeting audiences for landing pages, thank-you pages, and full sites. Config storage + retrieval foundation only — NOT wired into every website generation path (avoids touching protected Tombstone workflows).
- **Schema** (additive, pushed to BOTH dev DB and Render prod DB): 5 models appended to `prisma/schema.prisma`:
  - `TrackingPixel` — name, platform, pixelType, trackingMethod, pixelId, scriptSnippet (stored, never executed in UI), placement, scope (site_wide/landing_page/thank_you_page/specific_pages), status, consent fields (consentRequired, consentCategory, firesBeforeConsent, regionRulesJson, cookieBannerRequired), platform-specific IDs (ga4MeasurementId, analyticsStreamId/PropertyId, googleTagId, googleAdsConversionId/Label, gtmContainerId, metaPixelId/DatasetId/ConversionsApiEnabled, tiktokPixelId, linkedinPartnerId, bingUetTagId, choozleAdvertiserId/PixelId/ConversionId, etc.), server-side fields (serverEventEnabled, deduplicationKeyStrategy, hashedEmail/PhoneEnabled, enhancedConversionsEnabled), verification fields, testModeEnabled, createdByUserId/updatedByUserId
  - `TrackingEvent` — eventName, pixelId (optional FK), eventType, trigger config, value/currency, requiresConsent, funnelStage
  - `TrackingAudience` — audienceName, platform, sourceEvent, includeRulesJson, excludeRulesJson, retentionDays, funnelStage (mid-funnel for landing pages, bottom-funnel for thank-you pages)
  - `TrackingEventRoute` — pageType, eventName, firesOn, platformsJson (maps page types → events → platforms)
  - `TrackingPixelAuditEvent` — append-only audit log (businessId, pixelId/eventId, action, details, userId)
  - Relations added to `Business` model: trackingPixels, trackingEvents, trackingAudiences, trackingEventRoutes, trackingPixelAuditEvents
- **Platforms**: meta, facebook, google_ads, ga4, google_tag, google_tag_manager, tiktok, linkedin, bing, microsoft_ads, pinterest, x, twitter, choozle, custom. Guided setups for GA4 (by Measurement ID), GTM, Meta, Choozle; generic custom snippet for others.
- **Lib helpers**:
  - `lib/tracking-pixels.ts` — enum constants (PLATFORMS, PIXEL_TYPES, PAGE_TYPES, etc.), `isOneOf`, validators (`validatePixelInput`, `validateEventInput`, `validateAudienceInput`), `sanitizeSnippetForDisplay` (HTML-escapes custom snippets so they NEVER execute in the UI), `logPixelAudit`, `getTrackingConfigForPage(businessId, {pageType, path})` with PAGE_TYPE_TO_SCOPES mapping + urlMatchesPattern
  - `lib/tracking-defaults.ts` — EVENT_TEMPLATES, AUDIENCE_TEMPLATES, ROUTE_TEMPLATES, buildGuidedSnippet()
  - `lib/tracking-auth.ts` — `resolveBusinessAccess(req, businessId)` (admin sees any business, others only own; returns 401/404 or {user, business}). Mirrors the services route auth pattern.
- **API routes** (all `force-dynamic`, business-scoped via resolveBusinessAccess, field-whitelisted, pixelId ownership validated, audit-logged):
  - `/api/businesses/[id]/tracking-pixels` (GET/POST), `/tracking-pixels/[pixelId]` (PATCH with action shortcuts verify/disable/enable/archive/test_event/fail_verification + full update; DELETE soft-archive, `?hard=1` hard delete)
  - `/api/businesses/[id]/tracking-events` (GET+templates/POST), `/tracking-events/[eventId]` (PATCH/DELETE)
  - `/api/businesses/[id]/tracking-audiences` (GET+templates/POST), `/tracking-audiences/[audienceId]` (PATCH/DELETE)
  - `/api/businesses/[id]/tracking-routes` (GET+templates/POST), `/tracking-routes/[routeId]` (PATCH/DELETE)
  - `/api/businesses/[id]/tracking-config` (GET ?pageType=&path= — page-scoped retrieval foundation)
  - `/api/businesses/[id]/tracking-audit` (GET activity log)
- **UI**: `app/dashboard/insights/tracking-pixels/page.tsx` (server wrapper) + `_components/tracking-pixels-manager.tsx` (5 tabs: Pixels, Events, Audiences, Page Rules, Activity; uses `useActiveBusiness`) + `_components/pixel-modal.tsx` (add/edit pixel with platform-specific guided fields + custom snippet textarea noting "never executed in UI" + placement/scope/consent + toggles).
- **Nav**: Desktop `Insights` converted from a Link to a dropdown — Unified Dashboard (→/dashboard/marketing) + disabled future items + Tracking Pixels (active). Mobile got an Insights section with Unified Dashboard + Tracking Pixels.
- **Verification**: manual status-only (no live pixel firing/verification from the app) — verification fields track status; no third-party API secrets required.
- **Isolation**: every route enforces business_id scoping; no cross-business leakage. No changes to social publishing, RSS, image generation, scheduler, Launch CRM provisioning, or Tombstone task lifecycle.

#### "Help Me Create a Pixel" Guided Wizard (June 2026)
- **Feature**: Multi-step guided wizard on the Tracking Pixels page for platforms Tombstone cannot auto-create (Meta via Launch CRM/GHL, etc.). Tells the customer exactly what to create, where, why, and where to paste the resulting ID/script — then auto-creates the internal Tombstone tracking records.
- **CRITICAL design principle (enforced in copy + plan builder)**: ONE base pixel/tag per business per platform. Funnel stages = events + page routes + audiences on that single pixel — NOT separate pixels. Meta especially: one Meta Pixel firing PageView on all pages, ViewContent on landing/service pages, Lead on thank-you pages; the "service page pixel" is the SAME pixel firing on service pages with an audience rule.
- **Schema** (additive, pushed to BOTH dev + Render prod DB): 7 new optional fields on `TrackingPixel`: `manualSetupRequired Boolean`, `setupStatus String?` (needs_setup|needs_customer_action|waiting_for_pixel_id|waiting_for_script|ready_to_install|installed|verified|failed_verification|inactive), `setupInstructionsJson Json?`, `platformSetupUrl String?`, `installationTarget String?` (tombstone_generated_site|ghl_funnel|ghl_website|wordpress|gtm|manual|unknown), `installStatus String?`, `installNotes String? @db.Text`. Lifecycle `status` field unchanged — wizard statuses live in the NEW setupStatus/installStatus.
- **Lib** `lib/tracking-wizard.ts`: pure data + helpers. `PLATFORM_CAPABILITIES` (11 platforms with capability flags — all currently guided-manual + manual-paste, none auto-create/detect yet), `TRACKING_GOALS` (7 goals), `RECOMMENDED_PLANS` per platform (summary, pixelCount=1, pixelPurpose, eventKeys, audienceKeys, notes), `getManualInstructions` (Meta 13-step + GHL/Launch CRM placement guide for Head Tracking Code; generic steps for others), `DISPLAY_COPY` (Meta/Choozle/goal copy), `buildWizardPlan(platform, goal, businessName)` → {pixel, events, audiences, routes, setupInstructionsJson}. 6 canonical audiences: All Website Visitors, Landing Page Visitors - No Conversion, Service Page Visitors, Thank You Page Visitors, Converted Leads, Converted Lead Exclusion Audience.
- **API** `POST /api/businesses/[id]/tracking-wizard` (also GET for metadata): modes `create_plan` | `have_id` | `save_setup_needed`. Atomically creates ONE base pixel + recommended events + audiences + routes in a `prisma.$transaction`, all business-scoped, audit-logged via logPixelAudit (action=created, details.source=wizard). With Pixel ID → status active + setupStatus ready_to_install; without → status draft + setupStatus needs_customer_action (never injects incomplete pixel). Platform id mapped into the correct column (metaPixelId, ga4MeasurementId, etc.) via capability.idField.
- **UI** `_components/pixel-wizard.tsx`: 5-step modal (Platform → Goal → Recommended Plan → Instructions → Create). Three action buttons on final step: "Create Tombstone Tracking Plan", "I already have the Pixel ID", "Save as setup needed". Entry points wired in `tracking-pixels-manager.tsx`: header button, empty-state button, and an Add Pixel split-button dropdown (Add manually / Help me create one / Detect existing [disabled, "Soon"]).
- **No unrelated changes**: no social publishing, RSS, image generation, scheduler, or Launch CRM provisioning logic touched. No script execution in admin UI (snippets stored only). All additive schema.

### Three-Tier Research Architecture & Search Intelligence (June 2026)
- **Goal**: Stop overloading the initial 3-post preview with deep research. Split research into 3 tiers with distinct triggers, depth, and storage.
  - **Tier 1 — Light Research**: fast/shallow; powers ONLY the first 3 preview posts; runs BEFORE full registration; max 3 pages; basic business info only. Must NEVER overwrite deep research.
  - **Tier 2 — Deep Research**: durable intelligence; runs AFTER registration/business claim + advertiser identity lock; full crawl, competitor research, pixel discovery, social voice, SEO, positioning. Gated by `canRunDeepResearch()` (requires Business.userId AND tombstoneBusinessId).
  - **Tier 3 — Ongoing Search Intelligence**: weekly/manual; keyword/rank/competitor tracking via compliant providers only (Ahrefs API, Google Search Console, Google Ads, Google Business Profile, approved SERP, manual import). NEVER runs during preview. No Google scraping.
- **Schema** (additive, pushed to BOTH dev + Render prod DB): 13 new models + research fields on Analysis (light/deep status + timestamps) + relations on Business. Models cover: deep research runs, tracking-pixel discoveries (discovery layer separate from TrackingPixel config layer), search intelligence runs, tracked keywords, tracked locations/service areas, organic rank history, paid ads observations, local pack observations, competitors, competitor movements, recommendations, search-intelligence settings, provider accounts.
- **Lib layer** (4 files): research tier orchestration, search-intelligence provider abstraction (Ahrefs/GSC/Google Ads/GBP/SERP/manual — graceful empty results when API key missing, live wiring deferred pending keys), pixel discovery + match/import/dedupe, `nextWeeklyRun()` scheduling helper.
- **Pixel discovery → config bridge**: deep research discovers pixels on the live site; UI "Detected on Website" section (in tracking-pixels-manager) offers Import/Confirm, Match to Existing, Ignore, Verify Again. Imported/auto-created TrackingPixels are ALWAYS draft/needs_verification only — never active. Flags configured-but-not-found pixels + event-gap recommendations.
- **API routes** (19): `/api/businesses/[id]/research/status` (returns `{light:{status,lastRunAt}, deep:{status,lastRunAt,canRun,reason}, ongoing:{enabled,lastRunAt,lastRunStatus,nextRunAt}}`), `/research/deep/run`, `/search-intelligence/run` + overview/keywords/locations/organic/paid/local-pack/competitors/movements/recommendations/settings/tracking-discoveries/[did]. ProviderAccount field is `provider` (not providerType).
- **UI**: `/dashboard/insights/search-intelligence` — 10-tab manager (Overview, Keywords, Service Areas, Organic, Paid Ads, Local Pack, Competitors, Movements, Recommendations, Settings) + embedded BusinessIntelligencePanel (3 cards: Light/Deep/Search Intelligence, deep buttons gated by `deep.canRun`). Header nav: "Search Intelligence" added to Insights dropdown above Tracking Pixels.
- **Scheduling**: weekly fields + `nextWeeklyRun()` + manual run exist in schema/lib; NO automated weekly daemon yet (offer Render cron / scheduled task on request).
- **Isolation**: every route enforces business_id scoping + advertiser identity lock; no cross-business leakage. Light never overwrites deep. No edits to /home/ubuntu/tombstone (Python backend consumes these endpoints).
- **Commit**: `2535020` on main.

### Two-Stage Website Workflow — Concept + Production (June 2026)
- **Goal**: Preserve TWO separate, persisted website states per business — **Concept Website** (validation/preview) and **Production Website** (real buildable site) — WITHOUT removing the existing concept workflow. Additive schema only; frontend + API only this phase (no Tombstone/Python edits).
- **State model** (String status fields w/ comments, NOT Prisma enums):
  - `website_stage`: concept | production
  - `concept_status`: not_started | generating | ready_for_review | revision_requested | approved | rejected | archived
  - `production_status`: not_started | waiting_for_concept_approval | planning | generating | qa_pending | qa_failed | ready_for_review | approved | published | archived
- **HARD RULE**: Production cannot start until `concept_status === 'approved'`, UNLESS an admin override (role==='admin'). Enforced server-side in `start-production` route (403) AND `canStartProduction()`.
- **Schema** (7 new models appended to `prisma/schema.prisma`, pushed additively via `prisma db push`, no data loss): `WebsiteProject` (per-business hub; websiteStage, conceptStatus, productionStatus, projectName, approvedConceptVersionId, currentProductionVersionId, pointers), `WebsiteConcept` (versioned concept records; status, html @db.Text, workflowId, analysisId, designDirectionJson, brandDirectionJson, approvedByUserId/At), `WebsiteProduction` (versioned production builds; status, planJson, qaSummary), `WebsitePage` (production pages; pageType home|service|location|about|contact, slug, title, status), `WebsiteSection` (per-page sections; sectionType, contentJson, order), `WebsiteAsset` (carries concept→production assets; assetType, url, scope), `WebsiteQaResult` (qaType concept_war_room|production_qa, verdict, scoreJson, gatesJson). Relations added to `Business`.
- **Lib**:
  - `lib/website-workflow.ts` — `WEBSITE_STAGE`/`CONCEPT_STATUS`/`PRODUCTION_STATUS` constants, `canStartProduction(conceptStatus, {adminOverride})`, `resolveBusinessAccess(email, businessId)` → `{user, isAdmin}` (admin=role==='admin', admins act on any business), `ensureWebsiteProject(businessId)` (idempotent hub creator).
  - `lib/website-production.ts` — `generateProductionFromConcept({businessId, websiteProjectId, conceptId})`: derives pages (home + service pages from confirmed/suggested/needs_review BusinessServiceOffering w/ seoEnabled + city/county pages for multi_location/regional w/ >1 location + about + contact), creates WebsiteProduction (planning→generating→ready_for_review), WebsitePage + WebsiteSection rows, carries concept assets → production assets, records WebsiteQaResult. Helpers `slugify()`, `buildPagePlan()`. NO publishing, NO tracking injection, NO GHL wiring.
- **API routes** (all force-dynamic, business-scoped, session-gated):
  - `/api/website-project` GET (ensure project; returns project+concepts+productions+pagesCount+qaResults+latestAnalysisId+isAdmin) / POST (set projectName)
  - `/api/website-project/[id]/approve-concept` POST — sets concept approved + project.approvedConceptVersionId
  - `/api/website-project/[id]/request-concept-revision` POST — concept→revision_requested, logs to SiteFeedback
  - `/api/website-project/[id]/start-production` POST — HARD GATE 403 unless approved or (adminOverride && isAdmin); calls generateProductionFromConcept
  - `/api/website-project/[id]/pages` GET production pages w/ sections
  - `/api/website-project/[id]/qa` GET all QA results
  - `/api/website-concept/sync` POST {businessId, workflowId, finalTaskId?, analysisId?} — pulls getConceptWebsiteStatus, persists/updates WebsiteConcept (ready_for_review when html present), records concept_war_room QA, updates project status (won't regress approved). Idempotent per (project, workflowId).
  - `/api/website-concept/[id]` GET concept record; `?html=1` serves concept HTML (powers "View Concept")
- **Concept sync hook**: `app/components/website-concept.tsx` — `syncConceptRecord()` best-effort POST to `/api/website-concept/sync` in 3 completion branches (completed-with-html, error-with-html, in-progress-with-html). Additive — existing generate/poll/concept-website-state routes untouched (back-compat preserved).
- **UI**: `app/dashboard/website/_components/website-section.tsx` REWRITTEN as two-stage workspace: status overview cards (concept + production badges), 6 tabs (Concepts / Production Site / Pages / Revisions / QA / Publish[disabled w/ Lock icon]), gated "Start Production Build" (disabled until conceptApproved) + admin override button, concept cards labeled "CONCEPT — NOT PRODUCTION" w/ View/Approve/Request Revision. Uses useActiveBusiness. "Generate Concept" routes to /results/{latestAnalysisId}.
- **NOT done this phase (intentionally per spec)**: no Blazing Hog site build, Publish tab disabled (publishing NOT enabled), no tracking/pixel injection, no GHL form wiring, no changes to unrelated workflows. Existing concept preview/generation fully preserved.
- **Tests**: `yarn tsc --noEmit` clean, `test_nextjs_project` passed (build OK, dev server 200), checkpoint saved.
